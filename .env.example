# 环境变量
# 模型/凭据与 pi 共享全局配置目录 ~/.pi/agent/（PI_CODING_AGENT_DIR 可覆盖）：
#   auth.json    — /login 保存的 API key / OAuth 凭据
#   models.json  — 自定义 provider/模型（Ollama/vLLM/代理等）
#   settings.json— retry（重试）、defaultModel、enabledModels
# 也可直接用各 provider 的标准环境变量（如 ANTHROPIC_API_KEY、OPENAI_API_KEY）。
