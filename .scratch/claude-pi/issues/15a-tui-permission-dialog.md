# 15a — TUI 权限弹窗

**What to build:** 权限门控第三级落地：危险操作弹出确认对话框（拒绝 / 允许 / 始终允许），选择结果进入权限规则持久化；非 TTY 模式（-p / --mode json）下权限行为按既有规则自动裁决（ADR-0003 语义，不弹窗）。

**Blocked by:** 14 TUI 核心

**Status:** ready-for-agent

- [ ] 危险操作触发弹窗，三种选择行为正确
- [ ] "始终允许"写入规则并后续生效
- [ ] 非 TTY 模式下无弹窗、按规则自动裁决
- [ ] 弹窗组件渲染与按键断言测试绿灯
