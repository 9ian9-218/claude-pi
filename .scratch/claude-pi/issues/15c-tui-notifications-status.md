# 15c — TUI 通知与状态

**What to build:** 事件流的可视化：后台任务完成通知渲染（06）、队友消息按 agent 配色区分（10，AGENT_COLORS 对齐 Python 版）、worktree 指示器（08，claim 时状态行显示隔离生效，complete 后恢复）。

**Blocked by:** 14 TUI 核心, 06 后台任务, 10 Teammates 核心, 08 任务看板 + worktree 隔离

**Status:** ready-for-agent

- [ ] 后台任务完成通知在 TUI 中可见
- [ ] 队友消息按角色配色显示
- [ ] worktree 隔离生效/解除时状态行指示正确
- [ ] 事件渲染的组件断言测试绿灯
